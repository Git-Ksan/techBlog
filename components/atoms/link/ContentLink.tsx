import styled  from "styled-components";

export const ContentLStyle = styled.a`
    color: #ff0000;

    font-size: 1rem;
    letter-spacing: 4px;
    font-family: sans-serif;
    margin-block-start: 1em;
    margin-block-end: 1em;
    overflow-wrap: break-word;

    &:hover {
        opacity: 0.5;
    }
    cursor: pointer;
`